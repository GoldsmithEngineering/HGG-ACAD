(defun c:CLOSETRAVERSE (/ i n x y ss)
  (if (setq ss (ssget '((0 . "AECC_COGO_POINT")))) ; If selection not empty
    (progn ; then
      (setq i 0
            n (sslength)
      )
      (while (< i n)
       
      )
  )
)
